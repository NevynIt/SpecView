"""
    load from and save to images
    use layers and color channels as colors (configurable)
    use x and y as time axis and freq/tone axis
    load/save metadata either in the file if possible (TIFF maybe) or as a sidecar file (full name.ext + additional ext)
"""